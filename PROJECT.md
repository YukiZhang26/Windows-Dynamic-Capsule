# Windows Dynamic Capsule

> 面向 Windows 11 的轻量实时任务胶囊，用一个可交互的顶部悬浮层，集中展示任务进度、媒体状态、计时器和经过筛选的重要通知。

## 1. 项目概述

Windows Dynamic Capsule 是一个常驻桌面的轻量应用。空闲时，它以小型胶囊停靠在屏幕顶部；当媒体状态、后台任务、计时器或通知发生变化时，它会短暂展开，显示必要的信息和操作。

它不是 Windows 通知中心的替代品，也不以复制 iPhone Dynamic Island 为目标。项目更关注 Windows 桌面上的实时任务感知、低打扰交互、全屏安全和开放的本地集成能力。

当前项目处于产品定义与技术验证阶段。

### 当前进度（2026-08-09）

- [x] 建立 .NET 10 WPF 解决方案和可复现的项目内 SDK 环境。
- [x] 实现顶部圆角胶囊窗口。
- [x] 验证紧凑窗口不抢占当前前台应用。
- [x] 验证 `WS_EX_NOACTIVATE`、`WS_EX_TOOLWINDOW` 和真实圆角窗口区域。
- [x] 验证 150% DPI 下的逻辑尺寸、物理尺寸和顶部定位。
- [x] 接入系统媒体会话、专辑封面和上一首/播放暂停/下一首控制。
- [x] 完成媒体紧凑态与展开态、260ms 宽高同步缓动和圆角封面布局。
- [x] 优化尺寸动画：动画帧只重定位不重复缩放 HWND，并把原生圆角区域重建降到动画结束时。
- [x] 加入歌词显示区域，并在媒体源未提供歌词时清晰降级。
- [x] 接入 LRCLIB 逐行时间轴歌词、会话缓存、请求限流和断网降级。
- [x] 实现 DWM 前台全屏判定和胶囊无焦点隐藏/恢复逻辑。
- [x] 实现活动窗口显示器跟随、DPI 二次校正和显示拓扑变化响应。
- [x] 验证单显示器 150% DPI 下三种胶囊布局的物理坐标。
- [ ] 在混合 DPI 多显示器真机上验证跨屏切换和显示器热插拔。
- [x] 增加 30 天、最多 256 首的本地歌词缓存，并在设置中提供可关闭的 QQ 音乐备用来源。
- [x] 使用 6 秒无边框全屏窗口完成应用级隐藏/恢复实测：测试前可见、全屏期间隐藏、退出后恢复。
- [x] 实现通知权限、增量监听、摘要解析、6 秒提醒和安全降级。
- [x] 验证系统通知监听 API 存在，并准备含所需能力的 MSIX 清单模板。
- [x] 设置页显示实时通知能力状态，并提供 Windows 通知设置入口和不依赖权限的本地预览。
- [x] 定义统一 `CapsuleEvent` 模型，并实现优先级、去重、过期和容量限制调度。
- [x] 使用仅限当前 Windows 用户的 Named Pipe 接入本地任务进度。
- [x] 将系统通知改为独立、点击穿透、4.5 秒自动收起的临时弹窗。
- [x] 实现两个运行任务的主次非对称分区；辅助任务可单击切换到主区。
- [x] 提供 PowerShell 任务发送脚本，并完成 NDJSON 管道传输与 Debug/Release 编译验证。
- [x] 增加系统托盘图标，以及暂停/恢复、设置和退出菜单。
- [x] 持久化显示器目标、顶部间距、动画和全屏隐藏设置。
- [x] 暂停状态统一控制窗口、全屏轮询和事件呈现；暂停期间仍接收并维护任务事件。
- [x] 增加配置大小、版本、字段和类型校验，以及损坏/缺失配置的默认值回退。
- [x] 增加独立 `EventPolicyEngine`、通知允许/屏蔽列表和四级事件隐私。
- [x] 增加托盘勿扰模式；勿扰期间保留媒体并暂停事件呈现。
- [x] 增加项目内 .NET 运行时启动脚本和启动退出码诊断。
- [x] 使用 PowerShell 数据探针验证 4 条来源规则和 4 个隐私级别。
- [x] 增加单实例内倒计时服务，支持运行、暂停、继续、取消、替换和完成状态。
- [x] 在托盘提供 5/15/25 分钟预设、自定义计时器及计时控制。
- [x] 增加 Pinned 调度：严重失败优先于固定事件，固定事件优先于普通高优先级事件。
- [x] 增加计时器进度、剩余时间、暂停状态、完成提醒和固定状态标识。
- [x] 在计时器主卡片中加入暂停/继续和取消功能键。
- [x] 使用纯数据探针验证 3 条计时状态迁移和 3 条固定优先级规则。
- [x] 使用 5 分钟启动计时器完成新版实际运行和顶部截图验证。
- [x] 使用受本机信任的测试签名 MSIX 验证用户授权、通知新增/移除和胶囊恢复空闲态。
- [ ] 在 Windows 隐私设置中手动撤销并恢复通知读取权限，验证运行时降级与重新授权。
- [x] 预留 Store 产品 `Windows Dynamic Capsule`，写入 Partner Center 正式包身份并生成 `0.1.0.0` x64 提交包。
- [x] 完成 `0.1.0.55 → 0.1.0.56 → 0.1.0.57` 升级、完整卸载、干净重装和设置哈希保留验收。
- [x] 通过本地任务管道完成任务 Alert 的动态视觉实测，进度环、标题和侧边收纳按钮显示正常。
- [x] 完成托盘菜单和设置窗口的动态交互实测；设置窗口改为非模态单实例，并验证运行中转发、持续显示和重复激活。
- [x] 设置、诊断和计时窗口与透明顶置胶囊解除 Owner 关系，消除附属窗口偶发空白合成问题。
- [x] 执行 `DynamicCapsule.CoreProbe`，验证主次任务调度、隐私策略和设置回退。
- [x] 公开 GitHub 仓库并启用私密漏洞报告、Dependabot 安全更新、Secret Scanning 和 Push Protection。
- [x] 增加 Windows CI 与 NuGet Dependabot 配置；本地等价的 Release 编译、CoreProbe 和 8 项仓库验证全部通过。

## 2. 产品定位

### 2.1 一句话定位

一个低干扰、可交互、可扩展的 Windows 顶部实时状态胶囊。

### 2.2 核心价值

- 把分散在不同应用里的实时状态集中到一个位置。
- 让用户在不切换窗口的情况下查看进度并执行简单操作。
- 为 Codex、下载器、构建工具和本地脚本提供统一的任务进度出口。
- 在游戏、视频、演示和屏幕共享时主动隐藏或遮蔽敏感内容。

### 2.3 非目标

第一版不做以下内容：

- 不替代 Windows 通知中心。
- 不读取聊天数据库、模拟键盘、注入其他进程或使用屏幕 OCR 抓取消息。
- 不承诺对所有聊天应用进行真实快速回复。
- 不开发插件市场、主题市场或复杂 AI 功能。
- 不在第一版支持任意脚本执行。

## 3. 目标用户与典型场景

### 3.1 目标用户

- 经常运行构建、下载、压缩、转码或 AI 任务的开发者。
- 希望减少窗口切换的办公用户和学生。
- 需要简洁媒体控制的用户。
- 经常进入游戏、视频、演示或屏幕共享模式的用户。

### 3.2 典型场景

1. Codex 完成一个任务后，胶囊显示任务名称和完成状态。
2. 下载或文件处理任务持续更新进度，用户可以查看百分比和预计时间。
3. 音乐切换时短暂显示歌曲信息，点击后展开播放控制。
4. 计时器结束时显示提醒，并提供稍后提醒或关闭操作。
5. 用户授权后，选定应用的重要通知以摘要形式显示。
6. 检测到全屏游戏、视频或演示时，胶囊立即隐藏。

## 4. 产品原则

| 原则 | 说明 |
| --- | --- |
| 少即是多 | 空闲时只保留小胶囊，事件到达后才展示更多内容。 |
| 不抢焦点 | 紧凑状态不激活窗口，不影响当前输入和窗口操作。 |
| 事件驱动 | 优先监听真实事件，避免高频轮询和持续动画。 |
| 隐私优先 | 默认只显示摘要，不持久化消息正文。 |
| 能力降级 | 权限不可用或接口不支持时，清楚说明并继续提供其他功能。 |
| 用户可控 | 位置、动画、来源、隐私、优先级和隐藏规则均可配置。 |

## 5. 版本范围

### 5.1 P0：核心纵向原型

用于验证产品体验和主要技术风险：

- 顶层透明悬浮窗口。
- Capsule、Alert、Expanded 和 Pinned 四种展示形态。
- 测试事件提供器。
- 本地任务进度事件。
- 系统媒体会话读取和播放控制。
- 事件队列、优先级、去重和自动收起。
- 托盘菜单和基础设置。
- 全屏隐藏。
- 多显示器和 DPI 缩放适配。
- 减少动画和纯色低性能模式。

### 5.2 P1：可发布 MVP

在 P0 稳定后加入：

- Windows 系统通知监听。
- 首次启动权限说明、授权、拒绝和撤销处理。
- 通知应用白名单与黑名单。
- 消息摘要、打开来源、复制内容和忽略操作。
- 隐私模式和屏幕共享模式。
- MSIX 打包、签名、开机启动和升级策略。
- 诊断信息与隐私安全日志。

### 5.3 P2：后续扩展

- 针对特定应用的回复插件。
- 插件 SDK 和版本化事件协议。
- 提醒事项、日历和剪贴板辅助操作。
- AI 任务摘要和候选回复，但不自动发送。
- 更多布局位置和主题。

## 6. UI 与交互设计

### 6.1 状态模型

状态拆分为三个独立维度，避免把隐藏原因、窗口尺寸和内容类型混在同一个枚举中。

#### 可见性

- `Visible`：正常显示。
- `FullscreenSuppressed`：全屏应用导致隐藏。
- `QuietSuppressed`：勿扰或屏幕共享导致隐藏。
- `Paused`：用户从托盘暂停。

#### 展示形态

- `Capsule`：静置胶囊。
- `Alert`：短暂事件卡片。
- `HoverPreview`：悬停预览，不获取键盘焦点。
- `Expanded`：可交互详情面板。
- `Pinned`：用户短时固定，暂不自动收起。

#### 内容类型

- `Idle`
- `Message`
- `Media`
- `TaskProgress`
- `Timer`
- `Reminder`
- `Error`

### 6.2 默认尺寸

所有尺寸使用设备无关像素（DIP），并根据显示器工作区自适应。

| 形态 | 建议尺寸 |
| --- | --- |
| Capsule | 宽 96–128，高 28–32 |
| Alert | 宽 340–520，高 76–128 |
| Expanded | 宽 480–760，高 160–360 |

Expanded 的最大宽度为 `min(760, 当前工作区宽度 - 32)`。

### 6.3 默认位置

- 默认位于当前活动显示器工作区顶部中央。
- 与屏幕工作区顶部保留 8–12 DIP 间距。
- 支持固定在主显示器或用户指定显示器。
- 每个显示器分别保存位置和缩放后的尺寸。

### 6.4 输入规则

- Capsule 和 Alert 不主动获取键盘焦点。
- 除可见胶囊区域外，悬浮窗口必须点击穿透。
- 悬停约 350 毫秒后进入 HoverPreview。
- 鼠标离开约 500 毫秒后收起。
- 单击进入 Expanded。
- 双任务时单击辅助小岛将其切换到主卡片。
- `Esc`、点击外部或操作完成后收起。
- Pinned 状态超时或用户取消后返回 Capsule。
- Expanded 关闭后把焦点还给原窗口。

### 6.5 动画规则

- Capsule → Alert：120–220 毫秒。
- Alert → Capsule：160–240 毫秒。
- Capsule → Expanded：220–320 毫秒。
- Expanded → Capsule：180–260 毫秒。
- 未展开紧凑态使用约 78% 不透明度的半透明深色表面；展开、任务和提醒态恢复高不透明度。
- 系统通知使用独立临时弹窗，默认显示 4.5 秒，不抢占主任务卡片。
- 减少动画模式只保留淡入淡出和简单尺寸变化。
- 全屏隐藏不播放复杂动画。

### 6.6 视觉规则

- 默认使用深色高对比胶囊，同时支持浅色模式。
- Acrylic 只用于短暂展开表面，并提供纯色回退。
- 不依赖模糊效果表达信息层级。
- 信息层级为：来源图标、标题、摘要、状态、操作。
- 使用系统图标和来源应用图标，避免来源不明。
- 所有操作都必须支持键盘和 UI Automation。

## 7. 事件与调度

### 7.1 标准事件模型

各来源先转换为内部 `CapsuleEvent`，调度器和 UI 不直接依赖来源协议：

```json
{
  "eventId": "task:codex:build-42",
  "kind": "TaskProgress",
  "priority": "Normal",
  "source": "codex",
  "title": "正在构建项目",
  "message": "正在编译主程序",
  "progress": 0.65,
  "taskState": "Running",
  "createdAt": "2026-07-29T15:30:00+08:00",
  "expiresAt": null
}
```

### 7.2 优先级

从高到低：

1. `critical`
2. `high`
3. `normal`
4. `low`

默认调度顺序为：

`Task Failed > Windows Notification / Task Completed > Running Task > Low-priority Event`

固定事件启用后，调度顺序调整为：

`Critical > Pinned > High > Normal > Low`

### 7.3 队列规则

- 相同 `eventId` 的事件更新现有卡片，不重复弹出。
- 同一时间展示一张主卡片；若还有运行中的任务或计时器，则在右侧显示较小的辅助区。
- 辅助区不与主卡片平分空间；主区负责完整信息和操作，辅助区只显示图标、标题与进度。
- 先按优先级、再按创建时间选择当前事件。
- 调度器最多保留 100 条事件；超限时先移除最旧的低优先级事件。
- 每 250 毫秒清理过期事件。
- 运行中的任务不自动过期；完成、取消默认显示 6 秒，失败显示 10 秒。
- Windows 通知不进入任务主次队列，使用 4.5 秒临时弹窗；同一时刻只保留最新一条。
- 点击 Alert 只移除当前胶囊事件，不修改来源系统中的通知或任务。
- 全屏抑制期间事件仍由调度器维护；恢复后只会显示尚未过期的事件。
- Pinned 内容不会被普通事件抢占；严重失败消失后会恢复原固定内容。
- 固定事件被取消、移除或过期时自动解除固定。

## 8. 来源与动作能力

### 8.1 本地任务

应用监听 `\\.\pipe\DynamicCapsule.v1`。服务端使用
`PipeOptions.CurrentUserOnly`，只接受相同 Windows 用户和权限级别的连接。
协议为一行一个 JSON 对象的 UTF-8 NDJSON；发送后客户端应关闭连接。

```json
{
  "version": 1,
  "type": "task.progress",
  "id": "build-42",
  "source": "codex",
  "title": "正在构建项目",
  "message": "正在编译主程序",
  "progress": 0.65,
  "priority": "normal",
  "timestamp": "2026-07-29T07:30:00.0000000+00:00"
}
```

支持的 `type`：

- `task.started`
- `task.progress`
- `task.completed`
- `task.failed`
- `task.cancelled`

约束：

- `version` 必须为 `1`。
- `id` 和 `source` 只允许字母、数字、点、下划线、冒号和连字符，最长 128 字符。
- `title` 最长 120 字符，`message` 最长 300 字符。
- `progress` 使用 `0` 到 `1` 的小数。
- 单行事件最大 16 KB；单连接最多处理 200 条事件。
- 同一运行中任务最多每 50 毫秒刷新一次。
- 无效类型、字段、优先级或 JSON 会被拒绝，不执行任何命令或 URI。

项目提供了发送脚本：

```powershell
.\scripts\send-task-event.ps1 `
  -Type task.progress `
  -Id build-42 `
  -Source codex `
  -Title "正在构建项目" `
  -Message "正在编译主程序" `
  -Progress 0.65
```

HTTP 回环接口暂未实现；如果以后为了兼容而加入，必须额外包含随机访问令牌和同等的大小、频率与字段校验。

### 8.2 媒体会话

通过 Windows 全局系统媒体传输控制会话读取：

- 标题、作者、封面和播放状态。
- 当前进度。
- 播放、暂停、上一首和下一首能力。

只显示来源真正支持的操作。

系统媒体会话通常不提供完整歌词。媒体源只负责提供歌曲元数据和播放时间轴，
`LyricsService` 使用歌名、歌手、专辑和时长从 LRCLIB 匹配逐行时间戳歌词。
请求串行执行并遵循服务端限流；同一运行会话内缓存匹配结果。网络不可用、
没有匹配结果或只有普通歌词时，媒体读取和控制不受影响，歌词区域显示明确的降级状态。

### 8.3 Windows 通知

- 使用 Windows 通知监听能力。
- 在 UI 线程发起权限申请。
- 运行前检查包身份；未打包版本明确提示需要 MSIX，不尝试绕过能力声明。
- 未打包开发版启动后通过托盘状态提示明确说明通知不可用；媒体和本地任务继续工作。
- 包清单声明 `uap3:userNotificationListener`，并在每次同步前重新检查用户授权。
- 启动时只建立现有通知 ID 基线，之后通过 `NotificationChanged` 同步新增和移除事件。
- 单条通知只保留应用名、标题、一行摘要和创建时间，不持久化正文。
- 新通知以 6 秒高优先级胶囊提醒覆盖媒体紧凑态，点击只收起本地提醒。
- 用户拒绝或撤销权限后进入降级模式。
- 设置页以状态卡片区分未打包、待授权、已授权、拒绝和不可用状态。
- 本地预览绕过包身份与来源列表，但沿用当前隐私级别、动画和临时弹窗视觉。
- 默认只启用用户选定的应用。
- 不尝试绕过系统通知权限。
- 不承诺通用快速回复。
- 默认不主动清空 Windows 通知中心。
- 只读取已经进入 Windows 通知中心的 Toast；来源应用自己的窗口内弹层不在监听范围内。

### 8.3.1 Windows“时钟”边界

- 当前倒计时由 `CountdownTimerService` 在灵动岛进程内维护。
- Windows 没有提供读取“时钟”应用活动计时器列表和实时剩余时间的受支持公共 API。
- `WindowsClockTimerSyncService` 以尽力而为方式读取已打开的“时钟 > 计时器”卡片，解析中英文时长和运行状态，并批量更新胶囊事件。
- 该同步依赖 Windows 时钟的可访问性控件结构；系统更新、语言、窗口未打开或页面不在计时器视图时可能暂时无法发现计时器。
- 内置倒计时继续作为稳定的本地能力；诊断页必须明确区分“已连接但暂无运行计时器”和“同步不可用”。
- 通知监听在取得权限后仍可接收“时钟”发出的完成 Toast，但不能单独还原运行中的实时倒计时。

### 8.4 动作适配

统一动作只包括：

- `open`
- `copy`
- `dismiss`
- `pause`
- `resume`
- `cancel`

回复、稍后提醒或其他高级动作由来源插件显式声明，不根据按钮文字猜测能力。

## 9. 技术架构

### 9.1 技术基线

- 主要平台：Windows 11。
- 兼容目标：Windows 10 作为尽力兼容，不作为主要测试基线。
- 语言与运行时：C#、.NET 10 LTS。
- UI：WPF。
- Windows 能力：Windows App SDK、WinRT 和必要的 Win32 API。
- 发布：MSIX 优先，同时保留开发阶段的非打包运行方式。

### 9.2 数据流

```text
Windows 通知 ─┐
系统媒体会话 ─┤
本地任务接口 ─┼─> Event Normalizer
测试提供器   ─┘          │
                         v
                  Policy Engine
            隐私 / 去重 / 优先级 / 全屏规则
                         │
                         v
                Scheduler + State Machine
                         │
                         v
                    Overlay Window
                         │
                         v
                   Action Adapter
```

### 9.3 建议模块

| 模块 | 职责 |
| --- | --- |
| `OverlayWindow` | 顶层窗口、点击穿透、显示器定位和视觉渲染。 |
| `PresentationStateMachine` | 控制 Capsule、Alert、Expanded 和 Pinned 转换。 |
| `EventNormalizer` | 把不同来源转换为标准事件。 |
| `EventPolicyEngine` | 处理通知允许/屏蔽列表，并把事件转换为指定隐私级别。 |
| `CapsuleEventScheduler` | 按事件 ID 去重，管理优先级、容量、过期和当前事件。 |
| `NotificationSource` | Windows 通知授权和同步。 |
| `MediaSource` | 系统媒体会话监听和控制。 |
| `LyricsService` | 匹配、解析、缓存并按媒体时间轴呈现逐行歌词。 |
| `LocalTaskPipeService` | 校验并接收当前用户通过 Named Pipe 发送的任务进度。 |
| `CountdownTimerService` | 管理单个倒计时的精确截止时间、暂停、继续、取消和完成事件。 |
| `FullscreenDetector` | 判断当前显示器和前台窗口的全屏状态。 |
| `ActionRouter` | 把 UI 操作分派到对应来源。 |
| `SettingsService` | 在 LocalAppData 中原子保存版本化设置，并对损坏配置安全降级。 |
| `TrayService` | 提供暂停/恢复、设置、状态提示和统一退出入口。 |

## 10. 隐私与安全

- 默认不保存消息正文。
- 默认只显示一至两行摘要。
- 提供 `full`、`summary`、`masked` 和 `iconOnly` 四级隐私。
- `summary` 将正文限制为 72 字符；`masked` 替换标题和正文；`iconOnly` 只保留来源和状态图标。
- 通知屏蔽列表优先于允许列表；允许列表为空时默认接受所有通知来源。
- 来源规则同时匹配稳定的 AppUserModelId 和用户可见的应用名称。
- 勿扰模式暂停事件呈现但不停止本地任务接收；关闭勿扰后恢复仍有效的事件。
- 锁屏、屏幕共享、演示和用户指定应用中隐藏敏感内容。
- 日志只记录事件类型、来源、时间、耗时和错误代码。
- 不记录访问令牌、消息正文、剪贴板内容或个人身份信息。
- 本地插件使用版本化协议和最小权限。
- 不允许事件载荷直接执行命令、脚本或任意 URI。

## 11. 全屏、多显示器与窗口行为

- 根据前台窗口、显示器工作区和用户黑白名单综合判断全屏。
- 全屏判断不使用截图、OCR 或扫描窗口内容。
- 使用 DWM 可见边界与显示器完整边界比较，避免把保留任务栏的普通最大化窗口判为全屏。
- 每 200ms 检查前台窗口，排除桌面、任务栏、隐藏窗口、最小化窗口和工具窗口。
- 退出全屏后使用 `SW_SHOWNOACTIVATE` 恢复胶囊。
- 全屏应用只影响其所在显示器；其他显示器由用户设置决定。
- 默认跟随当前前台窗口所在显示器；桌面位于前台时使用鼠标所在显示器。
- 响应 `WM_DISPLAYCHANGE`、`WM_SETTINGCHANGE` 和 `WM_DPICHANGED`，跨屏后重新读取窗口 DPI 并修正物理尺寸。
- 支持主显示器、当前活动显示器和固定显示器三种模式。
- 响应显示器连接、断开、分辨率和 DPI 改变。
- 远程桌面环境自动关闭高成本背景效果。
- 紧凑窗口不出现在任务栏和 Alt+Tab 列表中。

## 12. 性能目标

性能数据必须在固定测试机上重复测量，并记录平均值和 P95。

| 指标 | MVP 目标 |
| --- | --- |
| 空闲 CPU | 连续 5 分钟平均值低于 1% |
| 内存 | 工作集 P95 低于 150 MB |
| 冷启动 | P95 低于 3 秒 |
| 本地事件延迟 | 事件进入到胶囊可见 P95 低于 150 毫秒 |
| 全屏隐藏 | 检测到全屏后 P95 在 250 毫秒内隐藏 |
| 动画帧率 | 普通设备优先保持 60 FPS |
| 队列稳定性 | 连续注入 200 个事件时无崩溃、重叠或无限增长 |

## 13. MVP 验收标准

### 13.1 窗口与交互

- 空闲胶囊不抢焦点。
- 胶囊以外区域不阻挡桌面点击。
- 点击可以展开，`Esc` 可以关闭。
- 收起后焦点返回原窗口。
- 100%、125%、150% 和 200% 缩放下清晰且位置正确。
- 双显示器、不同 DPI 和显示器热插拔后位置正确。

### 13.2 事件

- 测试消息、媒体和任务进度可以正常展示。
- 相同任务进度更新不会产生重复卡片。
- 高优先级事件能够抢占普通事件。
- 过期事件不会在退出全屏后重新弹出。
- 来源不支持的操作不会显示。

### 13.3 隐私与全屏

- 隐私模式不显示联系人和正文。
- 全屏游戏或视频时悬浮窗口按规则隐藏。
- 退出全屏后只补发仍有效的重要摘要。
- 未取得通知权限时，其他来源仍能正常工作。

### 13.4 稳定性

- 托盘可以暂停、恢复、打开设置和退出。
- 应用异常退出后不会留下不可见的顶层窗口。
- 开机启动可以启用和关闭。
- 诊断日志不包含敏感正文。

## 14. 开发阶段

### 阶段 0：技术验证

目标：在投入完整 UI 前排除最主要的技术风险。

- 创建 .NET 10 WPF 项目。
- 实现不抢焦点、可点击穿透的顶层窗口。
- 验证透明背景、圆角、阴影和纯色回退。
- 验证跨显示器 DPI 和定位。
- 验证系统媒体会话读取与控制。
- 验证通知监听能力、MSIX 清单和用户授权。
- 验证全屏检测。

完成条件：以上能力分别有可运行的最小演示和记录的结论。

### 阶段 1：胶囊外壳

- [x] 建立主题、视觉令牌和基础控件。
- [x] 实现 Capsule、媒体 Expanded 和事件 Alert 形态与动画。
- [x] 实现托盘和基础设置框架。
- [x] 增加关闭动画设置。
- [x] 完成 Pinned 事件。
- [x] 完成键盘导航、高对比度和系统减少动画支持。

### 阶段 2：事件核心

- [x] 定义统一事件模型。
- [x] 实现优先级、去重、容量和过期调度。
- [x] 让通知和任务进入统一 Alert 呈现路径。
- [x] 把来源和隐私规则抽成独立 Policy Engine。
- [x] 增加勿扰抑制和恢复。
- 加入测试提供器和压力测试。
- [x] 完成 Pinned 和勿扰抑制恢复规则。
- [x] 完成批量合并规则：同一批事件按 `eventId` 原子更新，同 ID 以后到值为准，并且整批最多触发一次界面呈现刷新。

### 阶段 3：首批集成

- [x] 接入本地任务进度。
- [x] 接入媒体会话。
- [x] 接入通知监听和权限流程。
- [x] 接入倒计时和托盘控制。
- 实现基础动作适配。

### 阶段 4：质量与发布

- 完成全屏、多显示器、隐私和低性能模式。
- [x] 完成当前用户单实例、参数转发、登录启动开关和诊断摘要。
- [x] 完成基础键盘导航、高对比度和减少动画适配。
- [x] 完成自包含 MSIX 打包、证书预检、参数化签名和验签流水线。
- 进行性能、完整无障碍和稳定性测试。
- [ ] 使用企业策略认可的证书完成正式签名、安装和升级验收。
- 编写用户说明和隐私说明。

## 15. 主要风险

| 风险 | 应对 |
| --- | --- |
| Windows 通知与胶囊重复提示 | 默认只启用选定应用，优先展示任务进度和媒体状态。 |
| 通知权限被拒绝或撤销 | 把通知来源视为可选模块，提供清晰的降级界面。 |
| 通用快速回复不可实现 | 只提供通用安全动作，真实回复由来源插件实现。 |
| 透明窗口影响性能 | 减少持续模糊和阴影，提供纯色与减少动画模式。 |
| 胶囊遮挡窗口顶部操作 | 支持位置调整、自动隐藏和点击穿透。 |
| 全屏检测误判 | 使用组合规则、进程黑白名单和用户手动游戏模式。 |
| 本地接口被滥用 | 使用 Named Pipe ACL 或带令牌的回环接口。 |
| 在线歌词源不可用或匹配错误 | 严格校验歌名、歌手和时长；缓存结果并提供无歌词降级。 |

## 16. 已采用的产品决策

1. 第一版以“任务进度 + 媒体”为首要价值，系统通知作为需要 MSIX 身份的可选来源。
2. Windows 11 是正式测试平台，Windows 10 只做尽力兼容。
3. 技术基线采用 .NET 10、WPF 和 MSIX。
4. 默认跟随活动窗口所在显示器，也允许固定到主显示器。
5. P0 使用纯色深色背景，交互和性能稳定后再评估 Acrylic。

## 17. 当前进度与建议的下一步

“单实例 + 登录启动 + 诊断状态”已经完成：

1. 使用当前用户命名互斥体阻止重复启动，并通过仅限当前用户的 Named Pipe 转发参数。
2. 第二次启动可把计时命令交给现有实例，辅助进程随后正常退出。
3. 设置中提供“登录 Windows 时启动”，默认关闭；只在用户保存该选项时写入当前用户启动项。
4. 托盘提供诊断摘要，显示媒体、通知、任务管道、计时器、当前事件、配置和启动项状态，不包含事件正文。
5. Debug 与 Release 构建均为 0 警告、0 错误；启动生命周期静态检查 11/11 通过，单实例转发、非模态设置窗口和重复激活已完成实际运行验证。

“键盘导航 + 高对比度 + 减少动画”已经完成：

1. 设置、计时和诊断窗口使用循环 Tab 顺序、默认/取消按钮、访问键和清晰的焦点边框。
2. 设置窗口支持 `Ctrl+S` 保存与 `Esc` 取消；计时窗口支持 `Enter` 开始；诊断窗口支持 `Ctrl+C` 复制不含正文的摘要。
3. 运行时监听 Windows 高对比度和界面动画偏好，动态替换系统颜色；系统减少动画开启时会覆盖应用动画开关并立即停止活动动画。
4. 托盘增加可用键盘访问的“展开/收起胶囊”入口，不改变胶囊默认不抢焦点的窗口行为。
5. 诊断摘要增加当前辅助功能状态；辅助功能检查 8/8 通过，未修改 Windows 辅助功能设置。

当前测试机此前曾由企业签名级别的 Code Integrity 策略（Policy ID `{0283ac0f-fff1-49ae-ada1-8a933130cad6}`）拦截新生成的程序集；当前未打包开发构建已经可以直接运行，并完成任务、设置和全屏动态验收。正式发布仍必须使用专用于 Dynamic Capsule、且与发布身份匹配的代码签名证书，不得通过关闭安全策略或复用其他项目证书解决。

“MSIX + 参数化签名与发布流水线”已经完成：

1. 使用仓库内固定版本的 `Microsoft.Windows.SDK.BuildTools 10.0.28000.2526`，无需全局安装 Visual Studio 或 Windows SDK。
2. 默认发布自包含的 .NET 10 `win-x64` 应用，生成图标、参数化清单、MSIX 和 SHA-256 元数据。
3. 证书预检要求当前用户证书库、可访问私钥、Code Signing EKU、有效期和清单 Publisher 精确匹配。
4. 有证书时先签名应用 EXE/DLL，再打包并签名 MSIX；默认加入 RFC 3161 时间戳，最后使用 SignTool 验签。
5. 已实际生成并重新解包验证 `WindowsDynamicCapsule_0.1.0.0_x64.unsigned.msix`，包内 472 个可执行载荷中 470 个具有有效 Microsoft 签名，仅项目自己的 EXE 和 DLL 等待正式发布签名。
6. 使用 Publisher 精确匹配且受本机信任的测试证书生成 `0.1.0.57`，应用 EXE/DLL 与 MSIX 均签名，并通过 DigiCert RFC 3161 时间戳和 SignTool 验签。
7. 已完成 `0.1.0.55 → 0.1.0.56 → 0.1.0.57` 升级、卸载、干净重装、通知新增/移除、设置哈希保留和包内附属窗口实测。
8. 打包流水线检查 12/12 通过；缺失证书会在清理目录或签名文件前安全停止。
9. 已添加 GitHub Windows CI：自动执行 Release 零警告编译、CoreProbe 和 8 项不依赖真实桌面交互的仓库验证。
10. 已启用私密漏洞报告、Dependabot 安全更新、Secret Scanning、Push Protection 和每周 NuGet 依赖检查。

正式发布前仍需完成以下验收：

1. 为 GitHub 直装版获取受公共信任、Subject 对应 Dynamic Capsule 发布身份的正式代码签名证书；Microsoft Store 包交由 Partner Center 签名。
2. 手动撤销并恢复通知读取权限，验证降级提示和重新授权路径。
3. 在混合 DPI 多显示器真机上补做 100%、150%、200% DPI、跨屏和热插拔验收。
4. 生成 1.0 候选包，完成 Windows App Certification Kit 与 Partner Center 认证后再发布正式 Release。

## 18. 参考资料

- [.NET 支持策略](https://dotnet.microsoft.com/en-us/platform/support/policy)
- [Windows 通知监听](https://learn.microsoft.com/en-us/windows/apps/develop/notifications/app-notifications/notification-listener)
- [应用能力声明](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/app-capability-declarations)
- [Windows App SDK 通知概览](https://learn.microsoft.com/en-us/windows/apps/develop/notifications/app-notifications/)
- [全局媒体会话管理器](https://learn.microsoft.com/en-us/uwp/api/windows.media.control.globalsystemmediatransportcontrolssessionmanager.requestasync)
- [LRCLIB API](https://lrclib.net/docs)
- [MonitorFromWindow](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-monitorfromwindow)
- [多显示器对象定位](https://learn.microsoft.com/en-us/windows/win32/gdi/positioning-objects-on-multiple-display-monitors)
- [WPF 多显示器 DPI](https://learn.microsoft.com/en-us/windows/win32/hidpi/declaring-managed-apps-dpi-aware)
- [Windows 系统背景材质](https://learn.microsoft.com/en-us/windows/apps/develop/ui/materials)
- [PipeOptions.CurrentUserOnly](https://learn.microsoft.com/en-us/dotnet/api/system.io.pipes.pipeoptions)
- [NamedPipeServerStream.WaitForConnectionAsync](https://learn.microsoft.com/en-us/dotnet/api/system.io.pipes.namedpipeserverstream.waitforconnectionasync)
